services:
  agentbox:
    build:
      context: .
      args:
        AGENTBOX_BUILD_REVISION: ${AGENTBOX_BUILD_REVISION:-unknown}
        AGENTBOX_BUILD_SOURCE: ${AGENTBOX_BUILD_SOURCE:-https://github.com/sloikodavid/agentbox}
        AGENTBOX_BUILD_VERSION: ${AGENTBOX_BUILD_VERSION:-unknown}
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - agentbox_data:/data

volumes:
  agentbox_data:
